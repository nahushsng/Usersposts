
<div class="container py-4">

<div class="p-5 mb-4 bg-light rounded-3">
  <div class="container-fluid py-5">
<h1 class="display-5 fw-bold"><%=  current_user.name  %>     </h1>
<%=  current_user.email  %> | <% if current_user.try(:admin) == true %> Admin/User <% else %> User <% end %>  

  </div>
  <h5> Registered Events </h5>
  <%= render 'users/events' %> 
</div>
<%= link_to "Edit Profile", edit_user_path(@user), class: "btn btn-primary btn-sm"  %> 
<footer class="pt-3 mt-4 text-muted border-top">
  &copy; 2022
</footer>
</div>